<tr id="<%= dom_id(member) %>">
  <td><%= member.name %></td>
  <td><%= member.age %> anos</td>
  <td><%= member.role %></td>
  <td>
    <label class="inline-flex items-center">
      <input type="checkbox" class="form-checkbox text-blue-600" <%= 'checked' if member.firm_in_faith %> disabled>
      <span class="ml-2"><%= member.firm_in_faith ? 'Sim' : 'Não' %></span>
    </label>
  </td>
</tr>
