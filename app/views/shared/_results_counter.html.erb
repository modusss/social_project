<!-- Results counter -->
<div class="mb-4">
  <p class="text-gray-600 text-md">
    <span class="font-semibold"><%= collection.total_count %></span> 
    <%= collection.total_count == 1 ? singular_label : plural_label %>
  </p>
</div>
