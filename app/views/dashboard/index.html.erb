<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
  <% Family.valid_food_basket_statuses.each do |status| %>
    <div class="bg-white rounded-lg shadow-sm p-6">
      <h3 class="text-lg font-medium text-gray-900 mb-2"><%= status_label(status) %></h3>
      <p class="text-3xl font-bold text-blue-600"><%= Family.where(food_basket_status: status).count %></p>
      <%= link_to "Ver famílias", families_path(food_basket_status: status), class: "mt-4 text-sm text-blue-600 hover:text-blue-800" %>
    </div>
  <% end %>
</div> 